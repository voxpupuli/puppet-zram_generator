---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Per-OS family"
    path: "osfamily/%{facts.os.family}.yaml"

  - name: "Common data"
    path: "common.yaml"
...
